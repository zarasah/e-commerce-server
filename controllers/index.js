const userCtrl = require('./userControllers');
const categoryCtrl = require('./categoryControllers');
const productCtrl = require('./productControllers');

module.exports = {
    userCtrl,
    categoryCtrl,
    productCtrl
}